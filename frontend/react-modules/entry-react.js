/**
 * React modules will include the css they require, so there is not a separate
 * file to define which css will be compiled. Just import the module js files
 * you need below. They will be bundled and added to the asset pipeline for you.
 */

import './ticket-component/ticket-component.js';
